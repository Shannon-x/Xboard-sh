; 工单附件上传体积上限。
; 与 App\Services\TicketAttachment\AttachmentConfig::HARD_MAX_SIZE_MB（20MB，base64 形态约 27MB）
; 及 config/octane.php 的 swoole.options.package_max_length（32MB）配套。
; PHP 官方镜像默认 post_max_size=8M，Laravel 的 ValidatePostSize 会按它直接返回 413。
upload_max_filesize = 32M
post_max_size = 32M
